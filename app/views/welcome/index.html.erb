 <h1>All Posts</h1>
 <%= @posts.each do |post| %>
   <div class="media">
     <div class="media-body">
       <h4 class="media-heading">
         <%# topics_path %>
         <%= link_to post.title, "/topics/#{post.topic.id}/posts/#{post.id}" %>
       </h4>
     </div>
   </div>
 <% end %>
